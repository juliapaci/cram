[package]
name = "cram"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    "lexer",
    "parser",
    "codegen"
]

[dependencies]
lexer = { path = "lexer" }
parser = { path = "parser" }
codegen = { path = "codegen" }
